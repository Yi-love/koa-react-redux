'use strict';
import React from 'react';
import ReactDOM from 'react-dom';
import Info from '../../app/info.jsx';

var attachElement = document.getElementById('react-app');

ReactDOM.render(<Info />,attachElement);